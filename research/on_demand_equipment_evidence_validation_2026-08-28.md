# On-Demand Equipment and Evidence Access Validation — 2026-08-28

After removal of the default Plan Context canvas, equipment editing remains reachable from the Profile destination. A seeded generated 390 × 844 capture scrolled directly to the Profile equipment editor and showed its automatic-stack constraint explanation, gym-access choices, equipment controls, and fixed six-slot navigation with no overlap.

Focused source-level coverage confirms Profile mounts `AthleteAboutMePanel`, whose Available equipment section remains present; Movement and Body Lab remain direct Explore destinations; the inspected catalog surface retains its exercise evidence card; and Body Lab retains its `Evidence context` and `View methodology` disclosures. The former default Plan Context markup remains absent.
