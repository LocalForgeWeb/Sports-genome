# Shorter Intro Handoff Validation — 2026-08-28

The silent document-level Sports Genome boot was shortened while preserving the existing staged supplied-video treatment. The video now reaches a held frame at approximately **1.33 seconds**, React mounting begins at approximately **1.22 seconds**, the splash respects a **1.38-second** minimum presentation window, and it dismisses through one **260 ms** opacity/visibility transition.

Generated 390 × 844 and 1366 × 768 browser captures confirmed that the upright S/DNA mark remains fully visible and centered before the wordmark, with the “Sports Genome” and “Decoding Performance” text readable at the final formation stage. The following generated workspace frames showed the Home canvas with its six fixed bottom destinations and no visible splash seam or persisting overlay.

Under a four-times CPU-throttled phone simulation, the formation-only window recorded **34 ms p95 frame pacing** with a **67 ms maximum sampled gap**. The supplied video was muted and held at 1.04 seconds in that constrained run before workspace mounting, so the shortened timing did not cause audio playback or an overrun past the held-frame boundary.

This is browser-based verification only. Physical iPhone hardware and Safari behavior remain explicitly unverified.
