# Push Stack Regression Validation — 2026-08-29

## Purpose

This record covers the reported Push-stack loss, difficult Training Day exercise discovery, ambiguous contribution-score language, and low-contrast mobile text. It documents generated-browser checks only; it does not claim physical iPhone or Safari validation.

| Check | Result | Notes |
|---|---|---|
| Saved Push stack reselect | Passed by regression | An active empty snapshot now restores the saved weekly Push entry rather than returning early with an empty stack. |
| Deliberate duplicate prescription | Passed in generated browser | One Barbell Bench Press remained present, then duplicated into a separately editable 1 × 1 entry. |
| Push finder, chest filter | Passed in generated 390 × 844 capture | The finder showed 61 matching Push-compatible options; direct Pectoralis major targets appeared first and retained full-row inspect plus Add actions. |
| Mobile context and top tabs | Passed in generated capture | The top header and workspace tabs were opaque rather than showing moving page content through them. |
| Score scope | Covered by focused regression | Contribution values are labeled as relative catalog planning inputs, not exercise quality, activation, strength, or athlete scores. |

## Scope boundary

The Push target index remains a configured catalog-planning index. It evaluates only the active split targets and is not a population rank, a measured activation value, a strength assessment, or a personalized readiness score. Missing-target suggestions are deterministic, split-compatible catalog options; they are not claims of athlete-specific superiority.

## Remaining device check

The 390 × 844 capture is a generated browser check. The real iPhone Safari/PWA experience, including installed-app cache and hardware-device text rendering, remains to be observed manually.
