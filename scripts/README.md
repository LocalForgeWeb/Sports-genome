# Catalog Generator

`build_exercise_catalog.mjs` converts the supplied list at `/home/ubuntu/upload/Pasted_content.txt` into the typed front-end catalog. It uses a transparent category-first taxonomy with name-level overrides for movement and muscle emphasis. The generated sport grades are decision-support heuristics, not performance predictions.
